<% if can? :read, Player.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :players]),
    label: t('players.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('players.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
